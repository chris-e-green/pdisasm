# ADR 0002: ID serialization

Canonical IDs remain typed inside the domain model. String forms are stable display and export encodings only; repository adapters translate legacy segment/procedure/address rows at metadata boundaries.
